# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.env
.venv/
venv/
ENV/
.python-version
.claude

# Data blobs are gitignored (too large), but the snapshot MANIFEST is version-controlled
# lineage/provenance (E-DQ10) — re-include just that file.
/data/*
!/data/raw/
/data/raw/*
!/data/raw/MANIFEST.json
/vgreen-charging-siting-data/
/outputs/

# Jupyter
.ipynb_checkpoints/

# VS Code
.vscode/
uv.lock